# 芥子博客环境变量配置示例
# 复制此文件为 .env 并根据实际部署环境修改配置

# 服务器端口配置
PORT=3001
API_PORT=3001

# 数据库配置
# 数据库文件路径，默认为 ./data/blog.db
DB_PATH=./data/blog.db

# 文件存储配置
# 上传文件存储路径，默认为 ./uploads
UPLOADS_PATH=./uploads

# 备份文件存储路径，默认为 ./backups
BACKUPS_PATH=./backups

# 前端服务器配置（用于开发环境）
# 前端服务器端口
FRONTEND_PORT=4173

# 生产环境配置
# NODE_ENV=production

# 其他配置
# 如果需要自定义工作目录
# WORK_DIR=/path/to/your/blog